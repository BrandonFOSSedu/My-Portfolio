#include <stdio.h>

int main() {

	float a, b;
	int x, y;

	printf("Please enter 2 numbers and press enter: ");

	scanf("%d%d", &x, &y);
	printf("The sum of those numbers: %i\n", x+y);

	printf("Please enter 2 decimal numbers and press enter: ");
	scanf("%f%f", &a, &b);

	printf("The sum of those numbers: %f\n", x+y);
	

	return 0;
}
